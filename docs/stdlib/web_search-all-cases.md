# web_search — all cases

Every case file under `src/clauz3/stdlib/web_search/tests/cases/`,
inlined. `_pass` cases must prove; `_fail` cases must not.

See the [curated walk-through](web_search.md) for the trusted module and
contract vocabulary.

{{ include_dir("src/clauz3/stdlib/web_search/tests/cases", glob="*.py") }}
