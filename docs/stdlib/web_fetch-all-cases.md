# web_fetch — all cases

Every case file under `src/clauz3/stdlib/web_fetch/tests/cases/`,
inlined. `_pass` cases must prove; `_fail` cases must not.

See the [curated walk-through](web_fetch.md) for the trusted module and
contract vocabulary.

{{ include_dir("src/clauz3/stdlib/web_fetch/tests/cases", glob="*.py") }}
