# editor — all cases

Every case file under `src/clauz3/stdlib/editor/tests/cases/`, inlined.
`_pass` cases must prove; `_fail` cases must not.

See the [curated walk-through](editor.md) for the trusted module and
contract vocabulary.

{{ include_dir("src/clauz3/stdlib/editor/tests/cases", glob="*.py") }}
