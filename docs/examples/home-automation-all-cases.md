# Home automation — all cases

Every case file under `examples/home-automation/cases/`, inlined. `_pass`
cases should prove; `_fail` cases should be rejected by the prover.

See the [curated walk-through](home-automation.md) for context on the
trusted module and contract vocabulary.

{{ include_dir("examples/home-automation/cases", glob="*.py") }}
