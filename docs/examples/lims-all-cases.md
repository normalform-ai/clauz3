# LIMS — all cases

Every case file under `examples/lims/cases/`, inlined. `_pass` cases
should prove; `_fail` cases should be rejected by the prover.

See the [curated walk-through](lims.md) for context on the trusted module
and contract vocabulary.

{{ include_dir("examples/lims/cases", glob="*.py") }}
