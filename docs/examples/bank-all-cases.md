# Bank — all cases

Every case file under `examples/bank/cases/`, inlined. `_pass` cases
should prove; `_fail` cases should be rejected by the prover.

See the [curated walk-through](bank.md) for context on the trusted module
and contract vocabulary.

{{ include_dir("examples/bank/cases", glob="*.py") }}
