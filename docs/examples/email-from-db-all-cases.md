# Email-from-DB — all cases

Every case file under `examples/email-from-db/cases/`, inlined. `_pass`
cases should prove; `_fail` cases should be rejected by the prover.

See the [curated walk-through](email-from-db.md) for context on the trusted
modules and contract vocabulary.

{{ include_dir("examples/email-from-db/cases", glob="*.py") }}
